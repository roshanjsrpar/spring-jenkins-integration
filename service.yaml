apiVersion: v1
kind: Service
metadata:
  name: spring-jenkins-integration-service
spec:
  selector:
    app: spring-jenkins-integration
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort